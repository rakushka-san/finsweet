import { StaticSourcePipe } from './static-source.pipe';

describe('StaticSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new StaticSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
